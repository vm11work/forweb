import 'package:flutter/material.dart';

class WebMaximum extends StatelessWidget {
  const WebMaximum({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('앱입니다.'),);
  }
}
